{
    "name": "sistema-mariachis",
    "version": "1.0.0",
    "scripts": {
      "build": "cd frontend/gestion-frontend && npm install && npm run build",
      "start": "cd backend && npm install && node server.js"
    }
  }
  